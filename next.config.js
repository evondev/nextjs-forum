/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "img.clerk.com",
      "utfs.io",
      "source.unsplash.com",
    ],
  },
};

module.exports = nextConfig;
